/**
 * Описывает соотношение каждого типа оптимизации к общему количеству итераций оптимизации на каждом шаше расчета
 * в процентах
 */
export interface OptimizationTypeRatios {
    gtm: number;

    bhp: number;
}
