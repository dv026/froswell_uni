export class WellPointMap {
    public readonly id: number;
    public readonly x: number;
    public readonly y: number;

    public constructor(id: number, x: number, y: number) {
        this.id = id;
        this.x = x;
        this.y = y;
    }
}
