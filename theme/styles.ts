const styles = {
    styles: {
        global: {
            // styles for the `body`
            body: {
                fontFamily: 'Inter, sans-serif'
            }
        }
    }
};

export default styles;
